import os
import sys

# Lấy đường dẫn thư mục gốc của ứng dụng
base_path = sys._MEIPASS if hasattr(sys, '_MEIPASS') else os.path.abspath(".")

# Xây dựng đường dẫn tới tài liệu Word
document_path = os.path.join(base_path, file_name)

# Tiếp tục làm việc với đường dẫn tài liệu Word
